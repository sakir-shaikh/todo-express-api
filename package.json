{
  "name": "chapter-4",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    
    "dev": "node --watch --env-file=.env  --experimental-sqlite ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "This is the dockerized full stack to-do application that is built on the Node.js, postgres as database, a prisma ORM, and JWT Authentication.",
  "dependencies": {
    "@prisma/client": "^6.8.2",
    "bcryptjs": "^3.0.2",
    "better-sqlite3": "^11.10.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.0",
    "prisma": "^6.8.2"
  }
}
